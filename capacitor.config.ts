import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ioniclogin',
  appName: 'ioniclogin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
